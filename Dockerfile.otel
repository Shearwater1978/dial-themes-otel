FROM nginx:1.29.6-otel

RUN rm /etc/nginx/conf.d/default.conf

COPY static /var/www

COPY nginx/nginx.conf /etc/nginx/nginx.conf

COPY nginx/themes.conf.template /etc/nginx/templates/themes.conf.template
COPY nginx/otel.conf.template /etc/nginx/templates/otel.conf.template

RUN mkdir -p /tmp/templates/ /var/cache/nginx /var/log/nginx /var/run /tmp/nginx && \
    chown -R nginx:nginx \
        /var/cache/nginx \
        /var/log/nginx \
        /etc/nginx/conf.d \
        /var/run \
        /tmp \
        /tmp/templates \
    && \
    chmod -R g+w \
        /var/cache/nginx \
        /var/log/nginx \
        /etc/nginx/conf.d \
        /var/run \
        /tmp \
        /tmp/templates

USER nginx